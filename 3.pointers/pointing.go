package main

/*
	Know how stack and heap memory work with pointers in go.

	If you create a variable inside a function, it is created on the stack.
	If you pass the address of that variable to another function, the variable is moved to the stack.
	If you return the pointer it will be moved to the heap.
*/
